spring:
  application:
    name: credit-service
  cloud:
    config:
      import-check:
        enabled: false

eureka:
  client:
    enabled: false

customer-service:
  base-url: http://localhost:8081
